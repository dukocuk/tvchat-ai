# Generated by `npm run package:tizen` + the signing step — not source.
# Only config.xml and icon.png are tracked. Each developer builds and
# signs with their own author certificate.
assets/
index.html
author-signature.xml
signature1.xml
*.wgt
