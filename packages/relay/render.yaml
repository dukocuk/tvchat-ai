services:
  - type: web
    name: tvchat-relay
    env: node
    plan: free
    rootDir: packages/relay
    buildCommand: npm install
    startCommand: node src/server.js
    healthCheckPath: /health
    envVars:
      - key: PORT
        value: 10000
